# IRSA: bind to an IAM role that has deploy/iam-policy.json attached.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: greenroute
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/greenroute-bedrock
