[project]
name = "greenroute"
version = "0.1.0"
license = { text = "MIT" }
description = "Carbon-aware router for LLM requests"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
